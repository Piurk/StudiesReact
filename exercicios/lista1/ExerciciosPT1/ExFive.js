import React from 'react';
import { View, Text, Image } from 'react-native';

var numeros = [5, 7, 8,2,3]

const lista = (nome) => {
    return lista;
}
// Eu tentei automatizar mas só ferrava o programa, tem alguma função javascript de organizar?
function Ex5() {
return (
<View>
<Text 
style={{color:"#0a0a0a", fontSize:20}}> {numeros[3]} {numeros[4]} {numeros[0]} {numeros[1]} {numeros[2]}</Text>
</View>
);
}
export default Ex5;