import React from 'react';
import { View, Text, Image } from 'react-native';


var lista = [(Math.floor(Math.random()*10)+1),(Math.floor(Math.random()*10)+1),(Math.floor(Math.random()*10)+1),(Math.floor(Math.random()*10)+1),(Math.floor(Math.random()*10)+1)]

const lista = (nome) => {
    return lista;
}

function Ex5() {
return (
<View>
<Text> {lista} </Text>
</View>
);
}
export default Ex5;